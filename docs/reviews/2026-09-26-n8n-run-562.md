# Review: n8n execution 562 and generated PPTX

Date: 2026-09-26. Evidence: user-supplied PPTX, successful n8n execution 562, Gemini output, Parse Structure output and rendered PNGs. The full execution remains only in ignored `tmp/`; no chat ID, API response or personal metadata is committed. PPTX SHA-256: `42A15516EB8F345FF5BB0DF759855B352B1474DA546EA54D654CDB83B17DDF69`.

## Structural result

- Execution completed through `Send a document1` without an exception.
- PPTX is a readable ZIP with `[Content_Types].xml`, `ppt/presentation.xml`, 10 slide XML files and 3 media files.
- Requested count and delivered count are both 10. All slides contain text or an image.
- PowerPoint rendered all 10 slides. No visible clipping or overlap was found.
- Sources and conclusion contain no image or large overlay. Slide XML shows only a thin separator before the content; later text objects remain above it.
- Trusted FSM metadata is preserved in the delivered file. The topic uses a separate display title without byte-slicing the full request.

## Confirmed contract defects

| Area | Gemini output | Parse Structure | Delivered PPTX | Result |
|---|---|---|---|---|
| Theme | `style=minimal_light` from FSM | Preserved | Dark deep-blue theme | Val Town ignores the selected style. |
| Visual plan | `needed`, `concept`, `query_en`, `placement` | Drops `concept`; never supplies `type` | Three stock photos | Semantic evidence is lost before image selection. |
| Statistics | One `value` string containing `89%` and an attribution sentence; no label, description or source object | Accepts the incomplete object | Number is omitted; slide becomes bullets plus photo | Silent fallback hides invalid statistics. |
| Definition | Only `definition.term` | Drops definition entirely | Ordinary three-item list | Definition layout does not preserve its required payload. |
| Comparison | Only `leftTitle` and `rightTitle`; no items | Preserves incomplete aliases | Renderer invents the sides from general bullets | Contract fields and rendered content do not match. |
| Two column | No `columns`; only three bullets | Does not reject missing columns | Ordinary vertical list | Layout name is accepted without layout data. |
| Cards | `{title, description}` | Preserved | Rendered | Legacy alias still differs from canonical `{title, text}`. |
| Timeline | `{date,title,description}` | Preserved | Three large rounded panels | Content survives, but composition is visually close to three cards. |
| Sources | Plain strings | Preserved | Readable list | Provenance cannot be validated structurally and URLs/DOIs are absent. |
| Conclusion | Three bullets; no takeaway cards | Preserved | Readable numbered list | Visually acceptable, but incompatible with canonical conclusion contract. |

## Content and visual review

The deck has a clear hierarchy, consistent title sizes, readable body text and a coherent sequence. Layout repetition is limited. The last two slides are clean and do not reproduce the historical overlay bug.

The images are technically usable but weak semantically. The cover shows a generic lecture hall, the problem slide shows generic laptop work, and the statistics slide shows students with a tablet. The third image does not explain a quantitative claim. Circular crops repeat on all image slides despite the requested placement diversity.

The `89%` claim must not pass production validation. It has no canonical source object and the renderer silently removes the number instead of rejecting the slide. The UNESCO guidance title is real, but the execution does not provide evidence that this document supports the stated percentage. The exact Andreev citation could not be confirmed through the exact-title search or Crossref query and must remain unverified until a DOI, journal URL or library record is supplied. Russell and Norvig is an identifiable real book, but the sources slide still needs structured author/year/URL fields.

## Root cause and next action

The connection problem is fixed; the current bottleneck is contract drift. The active n8n workflow still asks Gemini for a legacy schema, `Parse Structure` silently removes required fields, and the legacy Val Town renderer accepts incomplete layouts. Updating the current Val Town code in place would remove the rollback path and can break the active bot.

Create a versioned renderer v2 endpoint, make the test workflow emit the canonical contract, reject this captured malformed shape before render, and compare a 10-slide output against this baseline. Switch production only after style selection, statistics provenance, sources and all distinct layouts pass end-to-end review.